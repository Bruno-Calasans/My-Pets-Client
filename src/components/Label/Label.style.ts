import styled from 'styled-components'

export const Container = styled.p`


`
export const Lab = styled.span`
    font-weight: bold;
    color: var(--primaryColor);

`
export const Content = styled.span`
    color: black;
`